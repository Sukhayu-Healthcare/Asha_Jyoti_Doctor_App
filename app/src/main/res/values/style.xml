<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Light-only MaterialComponents theme (app-wide). -->
    <style name="Theme.ASHA_JYOTI_DOCTOR_APP" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Core colors (tweak if you want) -->
        <item name="colorPrimary">#0B4470</item>
        <item name="colorPrimaryVariant">#072440</item>
        <item name="colorOnPrimary">#FFFFFF</item>

        <item name="colorSecondary">#6D376D</item>
        <item name="colorOnSecondary">#FFFFFF</item>

        <!-- Ensure widgets use MaterialComponents styles -->
        <item name="materialButtonStyle">@style/Widget.MaterialComponents.Button</item>

        <!-- Prevent Android 10+ Force Dark from flipping colors -->
        <item name="android:forceDarkAllowed">false</item>

        <!-- Optionally force light system UI appearances (status/navigation) -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">?attr/colorPrimary</item>
    </style>

</resources>
